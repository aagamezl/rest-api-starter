export namespace info {
    const openapi: string;
    namespace info {
        const title: string;
        const description: string;
        const version: string;
        namespace contact {
            const name: string;
            const email: string;
            const url: string;
        }
    }
}
