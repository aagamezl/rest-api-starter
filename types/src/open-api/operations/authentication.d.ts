export namespace authenticationHeader {
    namespace securitySchemes {
        namespace bearerAuth {
            const type: string;
            const scheme: string;
            const bearerFormat: string;
            const description: string;
        }
    }
}
